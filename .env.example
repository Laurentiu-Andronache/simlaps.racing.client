# SimLaps Client Configuration
#
# Source runs discover this file through python-dotenv. A frozen executable
# checks only for an external .env beside the executable; the release build
# never bundles or copies this file.
# Keep APP_SECRET empty unless a project maintainer explicitly authorizes a
# submission test. Never commit a real APP_SECRET or distribute one with a
# release.
APP_SECRET=
