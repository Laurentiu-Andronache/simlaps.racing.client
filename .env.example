# SimLaps Client Configuration
#
# The release build does not read or bundle this file. Keep APP_SECRET empty
# unless a project maintainer explicitly authorizes a submission test and
# provisions a temporary value in that test process's environment.
# Never commit a real APP_SECRET or use this template to configure a release.
APP_SECRET=
